# Basile UI Beta (v0.2.2)

![React](https://img.shields.io/badge/React-19.0-61DAFB?style=flat&logo=react) ![Tailwind](https://img.shields.io/badge/Tailwind-4.0-38B2AC?style=flat&logo=tailwindcss) ![License](https://img.shields.io/badge/License-MIT-green)

Basile UI 是一个基于 **React** 和 **Tailwind CSS** 构建的现代化 AI 聊天界面组件。v0.2.2 版本采用了全新的**模块化架构**，将核心功能解耦，提供更佳的可维护性和扩展性。

## ✨ 核心特性

*   **💎 Glassmorphism 设计**: 精美的磨砂玻璃视觉风格，支持明亮/深色模式自动切换。
*   **🧩 模块化架构**: 核心逻辑已拆分为独立的 Hooks 和 Components (`src/components`, `src/hooks`)，告别“面条代码”。
*   **📝 强大的内容渲染**:
    *   **Markdown**: 支持表格、列表、引用、任务列表等。
    *   **代码高亮**: 集成 PrismJS，支持多种语言语法高亮与一键复制。
    *   **数学公式**: 集成 KaTeX，完美渲染 LaTeX 数学公式（行内及块级）。
*   **🤖 AI 交互模拟**:
    *   **流式响应**: 模拟真实的 AI 打字机效果。
    *   **多模型切换**: 支持 Fast / Thinking / Pro 三种模式切换 UI。
    *   **多模态输入**: 支持文本、语音（模拟）、图片上传（模拟）。
*   **📱 响应式布局**: 完美适配桌面端与移动端，侧边栏可折叠。

## 🛠️ 技术栈

*   **框架**: [React 19](https://react.dev/) + [Vite](https://vitejs.dev/)
*   **样式**: [Tailwind CSS 4](https://tailwindcss.com/) + PostCSS
*   **图标**: [Lucide React](https://lucide.dev/)
*   **外部库**: PrismJS (代码高亮), KaTeX (数学公式) - *通过 `useExternalLib` Hook 动态加载*

## 📂 项目结构

```bash
basile-ui-v0.2.2/
├── src/
│   ├── components/      # UI 组件库
│   │   ├── Messages.jsx       # 消息气泡 (User/AI)
│   │   ├── InputArea.jsx      # 输入框与发送逻辑
│   │   ├── Sidebar.jsx        # 侧边栏与历史记录
│   │   ├── CodeCanvas.jsx     # 代码块渲染组件
│   │   ├── LatexRenderer.jsx  # 数学公式渲染组件
│   │   ├── MarkdownTable.jsx  # 表格渲染组件
│   │   ├── Modal.jsx          # 通用模态框
│   │   └── Toast.jsx          # 消息提示组件
│   ├── hooks/
│   │   └── useExternalLib.js  # 动态加载 PrismJS/KaTeX 脚本
│   ├── App.jsx          # 主应用逻辑（状态管理）
│   ├── main.jsx         # 入口文件
│   └── index.css        # 全局样式与 Tailwind 指令
├── tailwind.config.js   # Tailwind 配置
└── vite.config.js       # Vite 配置
```

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

访问 `http://localhost:5173` 即可查看预览。

### 3. 构建生产版本

```bash
npm run build
```

## ⚙️ 配置说明

### 🔑 启用真实 AI (Gemini API)

目前项目默认使用模拟数据。要启用真实的 Google Gemini API，请修改 `src/App.jsx`:

1.  打开 `src/App.jsx`
2.  找到 `handleSend` 函数
3.  填入你的 API Key:

```javascript
// src/App.jsx
const apiKey = "YOUR_GEMINI_API_KEY_HERE"; 
```

### 🎨 自定义样式

所有自定义动画（如 `animate-blob`, `animate-pop`）和 Glassmorphism 类（如 `.glass`, `.glass-sidebar`）均定义在 `src/index.css` 的 `@layer components` 中，方便统一管理和修改。

## 📝 开发日志

*   **v0.2.2 (Current)**: 代码解耦重构，拆分各个子组件，优化目录结构。
*   **v0.2.1**: 修复移动端适配问题。
*   **v0.2.0**: 初始版本，单文件组件。

---

*Built with ❤️ by Antigravity*
